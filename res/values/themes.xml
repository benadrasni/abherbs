<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- the theme applied to the application or activity -->
    <style name="HerbsActionBarTheme"
           parent="@style/Theme.AppCompat.Light">
        <item name="android:actionBarStyle">@style/HerbsActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/HerbsActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="HerbsActionBar"
           parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/MenuGrey</item>

        <!-- Support library compatibility -->
        <item name="background">@color/MenuWhite</item>
    </style>
</resources>